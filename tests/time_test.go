package tests

import "testing"

func TestAccessChallenge(t *testing.T) {
	//How long time access to a challenge when x amount do at the same time vs. when theres 1 one person.

	setupLog("Access Challenge")
	//now for testi westi
}

func TestAccessGuacamole(t *testing.T) {
	//How long time access to a “guacamole “when x amount do at the same time vs. when theres 1 one person

	setupLog("Access Guacamole")
	//now for testi westi
}

func TestAccessWireguard(t *testing.T) {
	//How long time access to a vpn via wireguard when x amount do at the same time vs. when theres 1 one person
	setupLog("Access wireguard")
	//now for testi westi
}
